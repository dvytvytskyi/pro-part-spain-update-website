// This file is now deprecated in favor of staticLocations.js
// But we keep it empty or minimal to avoid breaking imports if any left (though we shouldn't have any).

export const locationNameMapping = {};

export const normalizeLocationName = (name) => name;
